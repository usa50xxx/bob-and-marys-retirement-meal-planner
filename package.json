{
  "name": "bob-and-marys-retirement-meal-planner",
  "version": "1.0.0",
  "private": true,
  "description": "Offline meal planning, inventory, grocery, receipt, and spending tools for Bob and Mary.",
  "scripts": {
    "test": "node work/food-engine.test.js && node work/generate-receipt-fixtures.mjs && node work/test-focused-meal-planner.js && node work/test-receipt-files.js",
    "images:optimize": "node work/optimize-ingredient-images.mjs",
    "android:sync": "cap sync android",
    "android:build": "cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "pdf-lib": "^1.17.1",
    "playwright": "^1.61.1",
    "sharp": "^0.35.3"
  }
}
